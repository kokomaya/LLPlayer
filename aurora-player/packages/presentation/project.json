{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "presentation",
  "sourceRoot": "packages/presentation/src",
  "projectType": "library",
  "tags": ["scope:core", "layer:presentation"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint src --max-warnings 0",
        "cwd": "packages/presentation"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json --noEmit",
        "cwd": "packages/presentation"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/presentation"
      }
    }
  }
}
