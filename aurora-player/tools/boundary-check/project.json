{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "boundary-check",
  "sourceRoot": "tools/boundary-check/src",
  "projectType": "application",
  "tags": ["scope:tool"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "tools/boundary-check"
      }
    }
  }
}
