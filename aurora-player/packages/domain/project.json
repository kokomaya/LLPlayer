{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "domain",
  "sourceRoot": "packages/domain/src",
  "projectType": "library",
  "tags": ["scope:core", "layer:kernel"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint src --max-warnings 0",
        "cwd": "packages/domain"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json --noEmit",
        "cwd": "packages/domain"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/domain"
      }
    }
  }
}
